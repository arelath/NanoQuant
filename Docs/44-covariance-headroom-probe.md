# Diagonal versus Full-Covariance Headroom Probe

**Date:** 2026-07-29
**Status:** harness prepared; GPU measurement queued
**Model:** pinned `google/gemma-3-1b-it` revision
`dcc83ea841ab6100d6b47a070329e1ba4cf78752`

## Question

The production objective weights input channels independently. It therefore
uses the diagonal of the input second moment and ignores correlations between
channels. Error-anatomy measurements already found that 63–93% of functional
error lies in the top 256 activation directions, which is qualitative evidence
that off-diagonal structure matters. It is not yet the controlled same-rank
decision bound requested by `NextQualityLevers.md` §7.

`tools/probe_covariance_headroom.py` supplies that bound. For each tested
factor group it compares:

1. the best real-valued rank-r reconstruction under the diagonal of a fit
   covariance; and
2. the best real-valued rank-r reconstruction under the regularized full fit
   covariance.

Both reconstructions use the same target weights, raw output-Fisher weights,
production 1-BPW rank, and held-out activation covariance. This isolates the
headroom attributable to input correlations instead of mixing it with binary
factor optimization.

## Scope

The pre-registered representative probe covers blocks 0, 12, and 24 and:

- fused Q/K/V;
- O projection;
- gate projection;
- up projection.

Their input axes are at most 1,152 wide (`o_proj` is 1,024 wide on this model).
Gate and up correctly reuse the same captured MLP input covariance. Down
projection is excluded from this cheap gate because
its 6,912-wide dense covariance would require about 182 MiB per matrix before
factorization workspaces and would turn the decision screen into a memory
benchmark. If the smaller-input aggregate passes, down can be tested with a
block-diagonal or low-rank-plus-diagonal representation.

## Protocol

- fit rows: 2,048;
- disjoint held-out rows: 2,048;
- held-out WikiText sequences generated by the pinned tokenizer;
- raw corrected CCE Fisher output importance;
- same production factor-cost calculation at 1.0 BPW;
- dense covariance symmetrization plus 1% mean-diagonal damping;
- real rank-r SVD floors in the diagonal and covariance-whitened spaces;
- evaluation under the undamped held-out second moment.

The promotion threshold is a 20% aggregate reduction in held-out error energy
for the full-covariance floor relative to the diagonal floor. Per-group results
are retained to show whether a gain is broad or localized.

The pinned snapshot's single safetensors file was checked for the representative
QKV, O, and MLP tensor names. The queued command is:

```powershell
$snapshot = 'C:\Users\pdykstra\.cache\huggingface\hub\models--google--gemma-3-1b-it\snapshots\dcc83ea841ab6100d6b47a070329e1ba4cf78752'
.\.venv\Scripts\python.exe tools\probe_covariance_headroom.py `
  --model "$snapshot\model.safetensors" `
  --snapshot $snapshot `
  --calibration-state evidence\m4\gemma-cce-fisher-state `
  --output evidence\m4\covariance-headroom-probe\blocks-0-12-24.json `
  --local-files-only
```

Passing this bound does not authorize a format change. It promotes either an
input-Hadamard screen or a runtime-compatible weighted-ADMM design. Failing the
bound closes both directions for the tested groups.

## Validation

CPU tests cover:

- complete group topology;
- equivalence of diagonal and covariance floors for a diagonal covariance;
- a correlated synthetic case with a known 90% rank-one error reduction;
- dimension validation.

The CUDA measurement remains queued behind Experiment 032 and the already
committed Fisher exponent screen.

## Result

Pending.
