# 2026-08-12 Improvement Review Theory Test Ledger

**Status:** completed. Every claim below names a discriminating retained test
and has an explicit confirmed or disproven verdict.

This ledger converts
[`8-12-26-ImprovementReview.md`](8-12-26-ImprovementReview.md) into falsifiable
claims. A useful local metric is not enough: a positive representation claim
must survive its stated held-out functional and exact-rate gates. Existing
evidence is reused only when its protocol actually matches the claim.

## Recommended directions

| ID | Falsifiable claim | Required evidence | Current verdict | Authoritative evidence |
| --- | --- | --- | --- | --- |
| R1 | Adaptive tail-aware checkpoint selection with a baseline escape path finds a correction that improves disjoint NLL and full KL, then completes packed/GGUF quality without changing the compressed representation. | Completed Experiment 048 campaign, immutable selection and confirmation slices, exact materialization, strict validation, and terminal quality. | **Confirmed.** Correction 2 (64 steps) passed selection and disjoint confirmation; the campaign completed and accepted the policy. This confirms the adaptive policy, not BF16 parity. | `evidence/048/campaign/selection-decision.json`, `evidence/048/campaign/confirmation-c4.json`, `evidence/048/campaign/campaign-summary.json`, `Results/048/048-adaptive-capability-correction-d2-gemma-3-1b-it-quality.md` |
| R2a | On the prescribed nine owners, mature ADMM plus the existing sign-search candidate family can be filtered by functional scoring into sign changes that jointly improve held-out owner/block output, full-model KL, NLL, and persisted-dtype replay. | Blocks 0/12/25 × QKV/gate/down; equal storage; incumbent/control/tabu; disjoint confirmation for selected arms. | **Disproven for the prescribed existing candidate family.** Matrix NRMSE improved for all nine. Only block-25 QKV improved held-out KL with confidence and improved owner output; on the disjoint confirmation its KL improved but NLL regressed from 3.717579 to 3.722600. No candidate satisfies the review's joint rule. | `evidence/052/real-block-tabu-b0-b12-b25-fair.json`, `evidence/052/real-block-tabu-b0-b12-b25-functional-by-owner.json`, `evidence/052/real-block-tabu-b25-qkv-functional-confirmation.json` |
| R2b | A common functional utility score is sufficiently predictive to replace reconstruction response in rank, codebook, retry, and outlier allocation. | Candidate menus with exact costs; fit/selection utility; disjoint composed confirmation; comparison with current allocator. | **Disproven for the review's cheap cached-activation/isolated-block score; exact suffix KL remains a gate, not a predictive allocator.** On the prescribed nine sign candidates, isolated owner block-output delta had Spearman correlation 0.0167 with full-model KL delta and -0.15 with NLL; directions matched only 5/9 and 2/9. The same failure reproduces across formats: 16-row sidecars beat columns on weighted residual at all three depths but lost held-out KL on all eight sequences, while Experiment 096's KL-anchor allocator regressed its composed control. Thus no tested common approximation has the recall needed to replace family-specific screening. Exact installed-candidate NLL/KL is still authoritative, but its cost prevents treating it as a cheap universal response curve. | `evidence/064/functional-proxy-recall-nine-owner.json`, `evidence/064/structured-int8-sidecar-{down-b0-b12-b25-v2,c4-validation456-8x512}.json`, `Docs/96-product-codebook-mixed-one-bpw-allocation.md`. |
| R3 | Coordinate end-to-end binary QAT with persisted signs, a sub-1% Hamming cap, continuous refit, and disjoint checkpoint selection improves full-model NLL and KL at unchanged rate. | Same retained factorization; immutable-sign control; bounded coordinate arm; equal bit identity; disjoint NLL/KL confirmation. | **Confirmed as an adaptive coordinate mechanism.** One retained gate owner at a time was trained for 32 end-to-end tail-aware KD steps with matched continuous updates. The binary arm used FP32 shadow margins aligned to the frozen signs, projected after every step into a 0.8% Hamming ball, and evaluated through the actual hardened BF16 signs. Block 0 failed the joint gate (KL better, NLL worse) and must roll back. Blocks 12 and 25 passed on rows 128–135 and reproduced on untouched rows 136–143: versus immutable-sign controls, block 12 improved KL by 0.011906 and NLL by 0.022847 at 0.219% movement; block 25 improved KL by 0.066242 and NLL by 0.119840 at 0.171% movement. No deployed bits change. This confirms selective coordinate QAT, not blanket sign unfreezing or a composed full-model policy. | `evidence/062/block{0,12,25}-gate-coordinate-binary-qat-32step-kl8.json`, `evidence/062/block{12,25}-gate-coordinate-binary-qat-32step-confirm-kl8.json`, `tools/probe_coordinate_binary_qat.py`. |
| R4a | Deterministic per-token tail-mass weights outperform the best fixed coefficient on held-out NLL/full KL without losing the six-task guardrail. | Frozen teacher statistics; predeclared mapping; fixed-coefficient controls; disjoint selection and final quality. | **Disproven for the predeclared confidence map.** At 32 matched steps, mapping teacher top-64 mass to weights 0.25–1.0 regressed C4 full KL by 0.022593 ± 0.008960 and NLL by 0.062302 ± 0.011156 versus fixed 0.5. It lost NLL on all eight sequences, so confirmation and task gates were not opened. | `evidence/063/adaptive-distillation-targets-c4-validation440-8x512.json`, `tools/probe_adaptive_distillation_targets.py`. |
| R4b | A compact two- or three-band tail target outperforms the single aggregate tail bucket. | Top-256 cache with exact target identity; single-bucket control; disjoint NLL/full KL and final task guardrail. | **Disproven for the top-64 / ranks-65–256 / remainder target.** The matched top-256 target plan regressed full KL by 0.040395 ± 0.003696 and NLL by 0.082207 ± 0.006130; every sequence regressed both metrics. | `evidence/063/adaptive-distillation-targets-c4-validation440-8x512.json`, `tools/probe_adaptive_distillation_targets.py`. |
| R4c | Variable-k top-p targets outperform fixed top-64 at comparable training/cache cost. | Maximum-k cache; deterministic threshold; matched training; disjoint NLL/full KL and cost receipt. | **Disproven for top-p=0.9 with maximum k=256.** The variable-prefix arm regressed full KL by 0.049227 ± 0.007602 and NLL by 0.092145 ± 0.008824; NLL lost on all eight sequences. | `evidence/063/adaptive-distillation-targets-c4-validation440-8x512.json`, `tools/probe_adaptive_distillation_targets.py`. |
| R4d | Adding a small fixed hard-label CE term improves capability retention over teacher-only tail-aware KD. | Fixed coefficient arm, teacher-only control, disjoint NLL/full KL, final six-task guardrail. | **Disproven for a fixed 10% next-token CE blend.** This was the closest alternative, but still regressed full KL by 0.007287 ± 0.004123 and NLL by 0.019958 ± 0.006698; it lost NLL on seven of eight sequences. | `evidence/063/adaptive-distillation-targets-c4-validation440-8x512.json`, `tools/probe_adaptive_distillation_targets.py`. |
| R5a | Joint exact-bit allocation across rank, INT8/BF16 outliers, scale precision, and representation options improves full-model quality at approximately 1 BPW. | Per-owner exact option costs; multiple-choice allocation; disjoint composed NLL/KL; final quality if promoted. | **Disproven as the proposed next mechanism with the available menu and score.** The repository already solved the exact multiple-choice rank/free-row/product-code menu at 0.999988 charged BPW. Its reconstruction objective selected unsafe sacrifices; adding functional floors required hand-protecting block 12, block 3, and block 1, and the Experiment-056-style KL utility variant still regressed composed KL and NLL. Adding the strongest outlier option as Experiment 058 improved perplexity from 248.37 to 223.21, but increased effective BPW from 1.02450 to 1.03733 and reduced the six-task mean from 0.4625 to 0.4603. The proposed scale-precision axis is not a real candidate in the current representation: factor scales are fixed 16-bit persisted fields, with no measured lower-precision option or exact replay contract. Since R2b found no predictive common utility and R5b rejected the new structured option family, a larger joint solve would merely re-optimize a known-unreliable proxy; it cannot satisfy the claim's functional requirement. Retain exact joint allocation infrastructure, but do not promote this menu as a quality gain. | `Docs/96-product-codebook-mixed-one-bpw-allocation.md`, `Results/057/*-quality.md`, `Results/058/*-quality.md`, R2b/R5b evidence above. |
| R5b | Row-segment or aligned-tile INT8 sidecars dominate whole-column sidecars at equal physical bits. | Same owner residuals; exact metadata/alignment costs; held-out functional splice on early/middle/late owners. | **Disproven for the proposed 16/32-row and 8×8/16×8 shapes.** On Experiment 058 down projections at blocks 0/12/25, 16- and 32-row segments used no more bits than one INT8 column and improved the retained weighted residual at all depths. That proxy gain did not transfer: on held-out C4 rows 448–455, 16-row segments regressed KL by 0.013350 with paired interval `[+0.005617,+0.024896]` and NLL by 0.009289 with interval `[+0.002012,+0.018059]` versus the column arm. The 8×8 and 16×8 tiles already lost the equal-rate reconstruction gate in all three blocks, and their functional point estimates also regressed both metrics. No structured arm qualifies for a runtime schema. | `evidence/064/structured-int8-sidecar-down-b0-b12-b25-v2.json`, `evidence/064/structured-int8-sidecar-v2-c4-validation456-8x512-final.json`, `tools/probe_structured_int8_sidecar.py`. |
| R6 | A 2×2 grouped middle core, with rank reduced to match all-in bits, improves held-out block output, full-model KL, and NLL on the nine prescribed owners. | Exact grouped least-squares fit; diagonal control; persisted-dtype dense replay; blocks 0/12/25 × QKV/gate/down; disjoint confirmation. | **Disproven at the required equal-rate reconstruction gate.** Fixed, magnitude, Gram-coupled, and residual-correlated pairing all regressed the block-12 gate canary. The least-bad residual pairing then regressed all nine prescribed owners by +0.3338% to +5.4829% weighted error after persisted-BF16 replay. The candidate therefore does not qualify for a functional splice or runtime work under the predeclared protocol. | `evidence/060/grouped-middle-core-block12-gate-canary.json`, `evidence/060/grouped-middle-core-block12-gate-{fixed,coupling,residual}.json`, `evidence/060/grouped-middle-core-nine-owner-residual.json`, `tools/probe_grouped_middle_core.py`. |
| R7a | Clean one-block subprocess isolation prevents process-local numerical contamination while preserving durable numerical identity. | Same recipe across a failing same-process boundary and clean-process resume; hash-valid durable artifacts; terminal campaign. | **Confirmed operationally.** Experiments 054 and 048 reproduced finite durable activations after process-local failures and completed through clean one-block workers. This is a containment result, not a root-cause proof. | `Docs/Experiments/054-functional-binary-learning-rate.md`, `Docs/83-experiment-048-paused-handoff.md`, Experiment 048 journal and terminal campaign. |
| R7b | Separate fit/selection activations, persisted-dtype epoch evaluation, and held-out best-state restoration improve production tuning versus legacy final-state semantics. | Matched tuning arms and optimizer excursion; disjoint owner/block and suffix metrics; persisted state identity. | **Disproven for the production-step-matched factorized gate schedule.** A 64-row × 32-epoch replay matches the 256 optimizer steps of the 256-row × 8-epoch production horizon. On blocks 0, 12, and 25, both fit loss and disjoint held-out BF16 block loss selected the final epoch. Consequently `legacy_final`, `fit_best`, and `heldout_best` restored the same persisted state, sign counts, short-splice KL, and NLL in every block. The safer API remains useful as a rollback guard, but it produces no quality gain on this tested schedule. | `evidence/061/block{0,12,25}-gate-heldout-checkpoint-policy-epochs32-kl{16,8,8}.json`, `tools/replay_gemma_gate_tuning.py`. |

## Explicit deprioritization claims

| ID | Claim | Verdict | Authoritative evidence |
| --- | --- | --- | --- |
| D1 | More direct binary-search tiers under the same reconstruction objective are not a reliable next campaign. | **Confirmed.** The exact nine-owner tabu screen improved matrix NRMSE everywhere but broad composition worsened held-out KL; the sole confirmed KL arm regressed NLL. | Experiment 052 evidence named under R2a. |
| D2 | Dense covariance refinement alone should remain rejected. | **Confirmed.** Both pre-tuning and selected post-refit placements completed real-model quality and regressed matched perplexity. | Experiment 033 and 034 quality reports and experiment documents. |
| D3 | Hadamard/random input rotations should remain rejected for the current factor format. | **Confirmed.** Every seed worsened held-out joint KL by 46.56%–57.35% at equal bits. | `Docs/45-input-hadamard-covariance-screen.md`, `evidence/m4/input-hadamard-probe/blocks-0-12-24-seeds-0-1-2.json`. |
| D4 | Over-complete rank should remain rejected under the current response objective. | **Confirmed.** Experiment 055 increased rank at essentially unchanged BPW, increased block time 87.8%, and worsened perplexity 40.06%. | `Docs/Experiments/055-overcomplete-rank-d2.md`, Experiment 055 quality report. |
| D5 | Broad reciprocal MLP factor fusions should remain rejected; QKV is the supported fusion. | **Confirmed.** Eleven of twelve reciprocal-MLP KL intervals were wholly worse and all operator-output comparisons regressed; the remaining interval crossed zero with a 76% output regression. | `Docs/84-experiment054-factor-fusion-retest.md` and its retained evidence. |
| D6 | Global bias correction and an unfunded low-rank patch should remain rejected. | **Confirmed for the tested policies.** The global bias arm and equal-budget rank-4 patch failed their functional/equal-budget gates. | `Docs/33-error-budget-driven-quality-improvements.md`, `Docs/Experiments/020-error-budget-and-kl-campaign.md`. |
| D7 | A blanket larger fixed outlier fraction is inferior to marginal allocation. | **Confirmed for blanket 0.2% versus 2% policies.** The larger policy improved perplexity but added about 0.146 BPW and did not improve mean task accuracy. This does not decide structured or jointly allocated sidecars. | `Docs/Experiments/011-gemma-3-1b-int8-outliers-0.2.md`, `Docs/Experiments/012-gemma-3-1b-int8-outliers-2.0.md`. |

## Remaining test order

No review theory remains open. The mechanisms supported for follow-up are the
completed Experiment 048 adaptive correction policy, clean per-block process
isolation, and selective coordinate binary QAT with atomic rollback. The tested
universal allocation proxy, richer KD targets, grouped core, held-out tuning
selector, larger joint menu, and structured sidecars are rejected under their
stated protocols.
